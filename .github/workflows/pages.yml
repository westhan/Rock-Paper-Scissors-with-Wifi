name: Deploy LifeMaxx to GitHub Pages

on:
  push:
    branches: [claude/lifemaxxing-habit-tracker-3gnrui]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Publish lifemaxxing/ to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./lifemaxxing
          publish_branch: gh-pages
